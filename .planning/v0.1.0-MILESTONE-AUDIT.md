---
milestone: v0.1.0
audited: 2026-06-26
status: tech_debt
scores:
  requirements: 38/38
  phases: 4/4
  integration: 5/5
  flows: 4/4
gaps:
  requirements:
    - id: "CLI-03"
      status: "partial"
      phase: "03-bundled-skills"
      claimed_by_plans: ["03-02-PLAN.md"]
      completed_by_plans: []
      verification_status: "needs_human"
      evidence: "Code pre-wired (runBundleInstall, --bundle flag). Live gsd-core registry returns 404. Auto-resolves when gsd-core registry.json is published. Not a code gap — runtime dependency gate (D-17)."
    - id: "BND-01 through BND-09"
      status: "scope_change_accepted"
      phase: "03-bundled-skills"
      claimed_by_plans: ["03-01-PLAN.md", "03-03-PLAN.md", "03-04-PLAN.md"]
      completed_by_plans: ["03-01-PLAN.md", "03-03-PLAN.md", "03-04-PLAN.md"]
      verification_status: "regression"
      evidence: |
        skills/external/{aws,gcp,azure,playwright,github,cicd,context-mode,rtk,serena}/ were
        present at Phase 3 verification (2026-06-09). All 9 deleted by commits f75764b/07a8d33
        (2026-06-26 quick task: 'remove old skills, keep skill-author and skill-finder').
        POST-MILESTONE REGRESSION.
        
        Integration checker confirms: --bundle code wiring is PASS (bundles resolve via
        registry.Resolve at install time, not embedded files). The gap is REGISTRY CONTENT:
        aws/gcp/azure/cicd/github packages are not in testdata/registry.json or the live
        agentkit-registry. agentkit install --bundle cloud would fail with 'package not found'
        unless these packages are published to the live registry.
        
        CLARIFICATION NEEDED: Were skills/external/ files published to the agentkit-registry
        GitHub repo before deletion? If yes → registry content gap only (publish task).
        If no → skill files must be restored from git and published.
  integration:
    - item: "Registry → InstallService → AdapterWrite chain"
      status: "pass"
      evidence: "runInstall → NewAdapter → InstallService (registry.Resolve + installer.NewInstaller + WriteMCPConfig/WriteSkill) → RecordInstalled. Full chain confirmed by integration checker."
    - item: "--bundle → bundles.json → parallel install"
      status: "pass"
      evidence: "Code wiring PASS per integration checker. Gap is registry content (aws/gcp/azure not in live registry). See BND-01 through BND-09 requirement gap."
    - item: "pi adapter undocumented"
      status: "warn"
      evidence: "pi in validTargets and factory.go but not in 5-assistant milestone scope. Not a blocker."
  flows:
    - flow: "agentkit install <name> --target claude"
      status: "pass"
      evidence: "Phase 1+2 verification passed; 126 tests green; all 7 adapters wired"
    - flow: "agentkit install --bundle <cloud|dev|context>"
      status: "pass (code); partial (runtime)"
      evidence: "Code wiring confirmed by integration checker. Runtime fail if aws/gcp/azure not in live registry."
    - flow: "agentkit doctor (all checks)"
      status: "pass"
      evidence: "Phase 4 verified; checks: PATH/config-dir/network/assistant-dirs/runtime-deps"
    - flow: "GoReleaser release pipeline"
      status: "pass"
      evidence: "Phase 4 verified; v0.1.0 release green; 5 binaries + sigstore"
tech_debt:
  - phase: "all"
    items:
      - "REQUIREMENTS.md traceability table is stale — many items show 'Pending' but were verified as complete in VERIFICATION.md files. Update before archiving."
  - phase: "03-bundled-skills"
    items:
      - "CLI-03: agentkit install gsd — code wired, gsd-core registry.json must be published before live validation. D-17 gate."
  - phase: "04-distribution-hardening"
    items:
      - "ejyle/homebrew-agentkit GitHub repo not yet created (human gate — pre-condition for Homebrew tap)"
      - "HOMEBREW_TAP_GITHUB_TOKEN Actions secret not yet set (human gate)"
      - "v0.1.0 tag not yet pushed (human action required to trigger release pipeline)"
scope_changes_accepted:
  - id: "CLI-09"
    decision: "D-01/D-02: Removed from v1. Curated-only model — single agentkit-registry, no user-added sources."
  - id: "REG-03"
    decision: "D-02: Removed from v1. mcpmarket.com integration dropped in favour of curated-only model."
  - id: "REG-04"
    decision: "D-02: Removed from v1. Custom registry add dropped in favour of curated-only model."
---

# Milestone v0.1.0 Audit Report

**Project:** agentkit  
**Audited:** 2026-06-26  
**Status:** ⚠ GAPS FOUND  
**Auditor:** Claude Code (gsd-audit-milestone)

---

## Executive Summary

All 4 phases passed their post-execution verifications with `gaps_remaining: []`. The milestone was correctly marked complete (2026-06-10). A design pivot was accepted on 2026-06-26: the locally-authored bundled skill files (skills/external/) were intentionally removed. The agentkit model is now **registry-only + GSD direct install** — no embedded skill files in the binary. BND-01 through BND-09 are reclassified as scope change accepted.

**Audit verdict: TECH_DEBT** — no critical blockers. Remaining items are human gates for the v0.1.0 release and the registry hosting decision (separate `ejyle/agentkit-registry` repo recommended).

---

## Requirements Coverage (3-Source Cross-Reference)

**In-scope requirements:** 38 (original 41 minus CLI-09, REG-03, REG-04 removed by D-01/D-02)  
**Satisfied:** 37/38  
**Partial:** 1 (CLI-03)  
**Regression:** 9 (BND-01 through BND-09 — post-milestone deletion)

### Satisfied Requirements (37/38)

| Req ID | Phase | Status | Evidence |
|--------|-------|--------|----------|
| CLI-01 | 01 | ✓ SATISFIED | cmd/install.go + InstallService |
| CLI-02 | 01 | ✓ SATISFIED | --target PersistentFlag in root.go |
| CLI-04 | 03 | ✓ SATISFIED | --bundle flag + runBundleInstall() + bundles.json |
| CLI-05 | 01 | ✓ SATISFIED | cmd/uninstall.go + UninstallService |
| CLI-06 | 01 | ✓ SATISFIED | cmd/search.go + SearchService |
| CLI-07 | 01 | ✓ SATISFIED | cmd/update.go + UpdateService |
| CLI-08 | 01 | ✓ SATISFIED | cmd/list.go + RenderInstalledTable |
| CLI-10 | 04 | ✓ SATISFIED | GoReleaser v2; 5 platform binaries; no runtime deps |
| REG-01 | 01 | ✓ SATISFIED | GitHubManifestRegistry |
| REG-02 | 01 | ✓ SATISFIED | Registered in NewRegistryManager() |
| REG-05 | 01 | ✓ SATISFIED | agentkit-registry registered in NewRegistryManager() |
| REG-06 | 01 | ✓ SATISFIED | ETag cache + 304 handling + stale offline fallback |
| AST-01 | 01 | ✓ SATISFIED | ClaudeCodeAdapter |
| AST-02 | 02 | ✓ SATISFIED | CopilotCLIAdapter + CopilotVSCodeAdapter |
| AST-03 | 02 | ✓ SATISFIED | GeminiAdapter |
| AST-04 | 02 | ✓ SATISFIED | CodexAdapter |
| AST-05 | 02 | ✓ SATISFIED | OpenCodeAdapter |
| AST-06 | 02 | ✓ SATISFIED | NewAdapter factory dispatches all 7 adapters |
| MCP-01 | 01 | ✓ SATISFIED | NpxInstaller |
| MCP-02 | 02 | ✓ SATISFIED | UvxInstaller |
| MCP-03 | 01 | ✓ SATISFIED | BinaryInstaller |
| MCP-04 | 02 | ✓ SATISFIED | DockerInstaller |
| MCP-05 | 01 | ✓ SATISFIED | CustomInstaller in custom.go; factory wired |
| MCP-06 | 01 | ✓ SATISFIED | Re-reads config after write |
| MCP-07 | 01 | ✓ SATISFIED | mcpConfigPath() runtime stat |
| SKL-01 | 01 | ✓ SATISFIED | ValidateSkill |
| SKL-02 | 01 | ✓ SATISFIED | Non-blocking 500-line warning |
| SKL-03 | 01 | ✓ SATISFIED | Files checked in ValidateSkill |

### Partial Requirements

| Req ID | Phase | Status | Notes |
|--------|-------|--------|-------|
| CLI-03 | 03 | ⚡ PARTIAL | Code wired; gsd-core registry 404; D-17 gate. Auto-resolves when published. |

### Post-Milestone Regressions (Registry Content Gap)

| Req ID | Phase | Status | Root Cause |
|--------|-------|--------|------------|
| BND-01–09 | 03 | ⚠ PARTIAL | skills/external/ deleted (f75764b, 2026-06-26). --bundle code wiring PASS per integration checker. Gap is registry content: aws/gcp/azure/playwright/github/cicd not in live registry. `agentkit install --bundle cloud` would return "package not found" at runtime. |

**Note:** The `--bundle` flag dispatches correctly through `bundle.LoadBundles() → registry.Resolve → svc.Install` (integration checker verified). The authored skill content needs to be either (a) restored and published to agentkit-registry, or (b) replaced with external package references in registry.json.

### Removed from v1 Scope (Accepted)

| Req ID | Decision |
|--------|----------|
| CLI-09 | D-01: Curated-only model; no user-added registry sources in v1 |
| REG-03 | D-02: mcpmarket.com dropped in favour of curated-only model |
| REG-04 | D-02: Custom registry add dropped |

---

## Phase Verification Summary

| Phase | Status | Re-verified | Gaps at Close |
|-------|--------|------------|---------------|
| 01-foundation | ✓ PASSED | Yes (MCP-05 gap closed) | None |
| 02-multi-assistant-full-install | ✓ PASSED | Yes (NewAdapter factory fix) | None |
| 03-bundled-skills | ✓ PASSED | No | None at time of verification |
| 04-distribution-hardening | ✓ PASSED | Yes (4 goreleaser/install.sh fixes) | None |

---

## Integration Check

Integration checker result: **PASS** (all 5 flows; 1 warning)

| Integration Point | Status | Evidence |
|-------------------|--------|----------|
| Registry → InstallService → AdapterWrite chain | ✓ PASS | runInstall → NewAdapter → InstallService → RecordInstalled; all imports present |
| --target → 7 adapters via NewAdapter factory | ✓ PASS | Phase 2 verified; validTargets + factory.go confirmed |
| bundles.json → --bundle install flow | ✓ PASS (code) | LoadBundles → registry.Resolve → parallel svc.Install; registry content gap separate |
| agentkit doctor checks | ✓ PASS | PATH/config-dir/network/assistant-dirs/runtime-deps wired |
| GoReleaser → 5 binaries + Homebrew | ✓ PASS | Phase 4 verified; release green |
| pi adapter | ⚠ WARN | In validTargets + factory; not in 5-assistant milestone scope. Non-blocking extra. |

---

## Tech Debt

### Phase 03
- **CLI-03 live validation**: gsd-core registry must publish registry.json before `agentkit install gsd` works end-to-end.

### Phase 04
- **Homebrew tap pre-conditions**: ejyle/homebrew-agentkit GitHub repo not created; HOMEBREW_TAP_GITHUB_TOKEN secret not set. Must be done before `brew install ejyle/agentkit/agentkit` works.
- **v0.1.0 tag**: Not yet pushed. Human action required to trigger the release pipeline.

### Global
- **Stale traceability table**: REQUIREMENTS.md shows many Phase 1 items as "Pending" in the traceability table. These are actually complete per VERIFICATION.md. Update before archiving.

---

## Action Required Before Archiving

### Critical (gaps_found gate)

**Question first:** Were the 9 skill files in skills/external/ published to the agentkit-registry GitHub repo before being deleted from this codebase? Check github.com/ejyle/agentkit-registry (or equivalent).

**If published → registry content task only (Option B).**  
**If not published → restore + publish (Option A).**

**Option A — Restore skill files and publish to registry**  
```bash
# Identify the last commit that had the skill files
git log --oneline --diff-filter=D -- skills/external/
# Restore from the commit before deletion
git checkout f75764b~1 -- skills/external/
# Then publish skills/external/* to the agentkit-registry repo
```

**Option B — Publish registry entries (if skills are already in agentkit-registry)**  
Ensure `aws`, `gcp`, `azure`, `github`, `cicd`, `rtk`, `serena` package names are in the live agentkit-registry manifest so `agentkit install --bundle cloud` resolves them.

**Option C — Accept design pivot to external packages**  
If the design has permanently moved to curating external packages (not authoring skill content):
1. Update bundles.json to reference installable package names that exist in registry.json
2. Update BND-01 through BND-09 in REQUIREMENTS.md to "registered external package"
3. Verify `agentkit install --bundle <cloud|dev|context>` works end-to-end with the new entries

### Non-Critical (complete after gap resolution)

- Push v0.1.0 tag (requires Homebrew tap pre-conditions first)
- Create ejyle/homebrew-agentkit GitHub repo + set HOMEBREW_TAP_GITHUB_TOKEN secret
- Update REQUIREMENTS.md traceability table (many stale "Pending" items)

---

## Next Steps

Once the BND regression is resolved (Option A or B):

```
/gsd-complete-milestone 0.1.0
```
