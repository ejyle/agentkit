---
phase: 03-bundled-skills
plan: 05
type: execute
wave: 4
depends_on:
  - 03-02
  - 03-03
  - 03-04
files_modified: []
autonomous: false
requirements:
  - CLI-03
  - CLI-04
  - BND-01
  - BND-02
  - BND-03
  - BND-04
  - BND-05
  - BND-06
  - BND-07
  - BND-08
  - BND-09

must_haves:
  truths:
    - "agentkit install --bundle cloud installs aws, gcp, azure and prints 3/3 installed"
    - "agentkit install gsd routes through gsd-core registry without error"
    - "Installed aws skill directory contains SKILL.md and references/ec2.md, s3.md, iam.md"
    - "All 10 skills exist under skills/ with valid frontmatter and under-500-line SKILL.md"
    - "go test ./... exits 0"
  artifacts: []
  key_links: []
---

<objective>
End-to-end human verification of Phase 3 success criteria. No code changes. Executor runs the build, runs tests, and the human validates the CLI behaviors described in ROADMAP.md Phase 3 success criteria 1–4.

Purpose: Gate before Phase 3 is marked complete. Provides a human-verified signal before Phase 4 begins.
Output: Approval or defect list that feeds gap-closure planning.
</objective>

<execution_context>
@/Users/nithin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nithin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/nithin/Ejyle/coding-agent-utils/.planning/ROADMAP.md
@/Users/nithin/Ejyle/coding-agent-utils/.planning/phases/03-bundled-skills/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated verification — build, test, content audit</name>
  <files></files>
  <read_first>
    - .planning/ROADMAP.md Phase 3 success criteria (4 items)
    - .planning/phases/03-bundled-skills/03-01-SUMMARY.md
    - .planning/phases/03-bundled-skills/03-02-SUMMARY.md
    - .planning/phases/03-bundled-skills/03-03-SUMMARY.md
    - .planning/phases/03-bundled-skills/03-04-SUMMARY.md
  </read_first>
  <action>
    Run the following checks and record results:

    1. go build ./... — must exit 0
    2. go test ./... — must exit 0; report total test count
    3. Content audit: for each of the 10 skill dirs (aws, gcp, azure, playwright, github, cicd, context-mode, rtk, serena, skill-author): verify SKILL.md exists, check line count (wc -l), verify references/ directory present, check frontmatter contains "name:" field
    4. Stub check: grep -r "TODO\|stub\|coming soon\|placeholder" skills/ — must return 0 matches
    5. agents/ check: verify agents/auto-researcher/AGENT.md exists
    6. build binary: go build -o /tmp/agentkit-test . — must succeed; run /tmp/agentkit-test install --help and verify --bundle flag appears in output
    7. CLI-03 check: run /tmp/agentkit-test install gsd --dry-run (if flag exists) OR verify via registry.NewRegistryManager() that gsd-core is pre-registered — document result

    Compile results as a verification table:
    | Check | Result |
    |-------|--------|
    | go build | pass/fail |
    | go test (N tests) | pass/fail |
    | All 10 SKILL.md present | pass/fail |
    | All SKILL.md under 500 lines | pass/fail |
    | All references/ present | pass/fail |
    | No stubs | pass/fail |
    | auto-researcher/AGENT.md present | pass/fail |
    | --bundle flag in install --help | pass/fail |
    | CLI-03 gsd routing confirmed | pass/fail |
  </action>
  <verify>
    <automated>go build ./... && go test ./... && echo "BUILD+TEST PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - go build ./... exits 0
    - go test ./... exits 0
    - All 10 SKILL.md files exist under skills/
    - grep -r "TODO\|stub\|coming soon\|placeholder" skills/ returns 0 matches
    - /tmp/agentkit-test install --help output contains "--bundle"
  </acceptance_criteria>
  <done>Automated checks all pass. Results table ready for human review.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Phase 3 complete: GitHubReleaseInstaller, --bundle command, 10 bundled skills, skill-author meta-skill, auto-researcher agent. Automated checks passed (build + tests + content audit).</what-built>
  <how-to-verify>
    Run these checks and confirm each passes:

    1. Build and install to a temp location:
       go build -o /tmp/agentkit .

    2. Verify --bundle flag:
       /tmp/agentkit install --help
       Expected: shows -b, --bundle flag in usage

    3. Verify bundle resolution (dry test — will fail at install step if registry not live, but bundle loading should work):
       /tmp/agentkit install --bundle cloud 2>&1 | head -5
       Expected: either "aws ✓ / gcp ✓ / azure ✓" (if registry live) OR a registry error (not a "bundle not found" error)

    4. Verify --bundle bad-name:
       /tmp/agentkit install --bundle nonexistent 2>&1
       Expected: error mentioning "not found" with available bundle names (cloud, dev, context)

    5. Verify skill content:
       ls skills/aws/references/
       Expected: ec2.md  iam.md  s3.md

       ls skills/gcp/references/
       Expected: cloudrun.md  compute.md  gke.md  iam.md

       ls skills/skill-author/
       Expected: SKILL.md  agents/ (or agents/ under repo root)  references/  scripts/

       ls agents/auto-researcher/
       Expected: AGENT.md

    6. Verify skill-author references:
       ls skills/skill-author/references/
       Expected: authoring-guide.md  evaluation-rubric.md  spec-compliance.md

    7. Verify all tests pass:
       go test ./... 2>&1 | tail -5
       Expected: ok lines, no FAIL lines

    8. Verify ROADMAP.md Phase 3 success criteria 1–4:
       - Criterion 1: agentkit install gsd works (or confirm gsd-core registry integration)
       - Criterion 2: --bundle cloud/dev/context flags work
       - Criterion 3: skill validator is called on install (check service/install.go)
       - Criterion 4: skills/aws/SKILL.md and skills/aws/references/ exist as expected
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 3, or describe any issues found for gap-closure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test binary → filesystem | The test binary is written to /tmp and does not modify production config |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-14 | Tampering | /tmp/agentkit-test binary | accept | Verification binary only; no production paths written during manual check |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package manager installs in this plan |
</threat_model>

<verification>
All ROADMAP.md Phase 3 success criteria verified by human.
go test ./... exits 0.
Human approved.
</verification>

<success_criteria>
Human confirms: --bundle flag present, bundle resolution works, all 10 skills exist with correct structure, go test passes. Phase 3 marked complete in STATE.md.
</success_criteria>

<output>
Create .planning/phases/03-bundled-skills/03-05-SUMMARY.md when done.
Update STATE.md: Phase 3 status = complete, progress = 75%.
</output>
