---
phase: 03-bundled-skills
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - internal/bundle/bundles.go
  - internal/bundle/bundles.json
  - internal/bundle/bundles_test.go
  - cmd/install.go
autonomous: true
requirements:
  - CLI-03
  - CLI-04

must_haves:
  truths:
    - "agentkit install --bundle cloud installs aws, gcp, azure in parallel and prints per-package result lines"
    - "agentkit install --bundle cloud exits with code 1 if any package fails, 0 if all succeed"
    - "agentkit install --bundle bad-name exits with a non-zero code and prints available bundle names"
    - "agentkit install gsd routes through gsd-core registry (no code change needed — verified in task)"
    - "agentkit install <name> (without --bundle) still works identically to Phase 2 behavior"
  artifacts:
    - path: "internal/bundle/bundles.go"
      provides: "LoadBundles() and Resolve(name) functions, BundleDef/BundleManifest types"
      exports: ["LoadBundles", "BundleManifest"]
    - path: "internal/bundle/bundles.json"
      provides: "cloud, dev, context bundle definitions"
      contains: '"cloud"'
    - path: "cmd/install.go"
      provides: "--bundle flag, runBundleInstall() function, cobra.RangeArgs(0,1)"
      contains: "runBundleInstall"
  key_links:
    - from: "cmd/install.go"
      to: "internal/bundle/bundles.go"
      via: "bundle.LoadBundles() call in runBundleInstall"
      pattern: "bundle.LoadBundles"
    - from: "cmd/install.go"
      to: "internal/service/install.go"
      via: "svc.Install() called per package in goroutine"
      pattern: "svc.Install"
---

<objective>
Implement the --bundle command: bundle definitions embedded in bundles.json, a LoadBundles/Resolve loader, and the --bundle flag on installCmd that launches parallel best-effort goroutines (sync.WaitGroup, NOT errgroup) and prints per-package results with exit code semantics per D-05.

Also verify CLI-03: confirm agentkit install gsd routes through the gsd-core registry already integrated in registry.NewRegistryManager() — document the gsd-core registry.json entry format in the summary (may be a no-op if it already works).

Purpose: Delivers the one-command bundle install capability that is the headline feature of Phase 3.
Output: internal/bundle/ package, cmd/install.go extended with --bundle flag and runBundleInstall.
</objective>

<execution_context>
@/Users/nithin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nithin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/nithin/Ejyle/coding-agent-utils/.planning/PROJECT.md
@/Users/nithin/Ejyle/coding-agent-utils/.planning/ROADMAP.md
@/Users/nithin/Ejyle/coding-agent-utils/.planning/phases/03-bundled-skills/03-CONTEXT.md
@/Users/nithin/Ejyle/coding-agent-utils/.planning/phases/03-bundled-skills/03-RESEARCH.md
@/Users/nithin/Ejyle/coding-agent-utils/.planning/phases/03-bundled-skills/03-PATTERNS.md

<interfaces>
From internal/bundle/bundles.go (to create — see PATTERNS.md for full source):
```go
package bundle

//go:embed bundles.json
var bundlesData []byte

type BundleDef struct { Packages []string `json:"packages"` }
type BundleManifest struct { Bundles map[string]BundleDef `json:"bundles"` }

func LoadBundles() (*BundleManifest, error)
func (m *BundleManifest) Resolve(name string) ([]string, error)
// Resolve error message: fmt.Errorf("bundle %q not found; available: cloud, dev, context", name)
```

From cmd/install.go (current — read before modifying):
```go
var installCmd = &cobra.Command{
    Args: cobra.ExactArgs(1),   // CHANGE to cobra.RangeArgs(0, 1)
    RunE: runInstall,
}
func init() { rootCmd.AddCommand(installCmd) }  // ADD bundle flag here
func runInstall(cmd *cobra.Command, args []string) error { ... }
// ADD at top of runInstall, before name := args[0]:
//   bundleName, _ := cmd.Flags().GetString("bundle")
//   if bundleName != "" { return runBundleInstall(cmd, bundleName, target, svc) }
```

Bundle result printing pattern (from PATTERNS.md / CONTEXT.md §Specifics):
- Per-package success: fmt.Printf("  %s ✓\n", r.name)
- Per-package failure: fmt.Fprintf(os.Stderr, "  %s ✗ %s\n", r.name, r.err)
- Summary success: fmt.Printf("%d/%d installed\n", len(pkgNames), len(pkgNames))
- Summary partial: fmt.Printf("%d/%d installed — %d failed\n", ok, total, failed) then os.Exit(1)

Parallelism: sync.WaitGroup + index-keyed results slice (NOT errgroup — errgroup cancels on first error, violating D-04). Pattern in PATTERNS.md §cmd/install.go.

From internal/registry/registry.go (for CLI-03 verification):
```go
func NewRegistryManager() *RegistryManager
// gsd-core registry is pre-registered as a default source
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create internal/bundle package with embedded bundles.json</name>
  <files>internal/bundle/bundles.go, internal/bundle/bundles.json, internal/bundle/bundles_test.go</files>
  <read_first>
    - internal/registry/local.go — JSON unmarshal from file pattern (lines 30-39) as structural analog
    - .planning/phases/03-bundled-skills/03-PATTERNS.md §internal/bundle/bundles.go — has complete source to mirror
    - .planning/phases/03-bundled-skills/03-RESEARCH.md §2 Bundle Command §bundles.json Schema — exact JSON schema
  </read_first>
  <action>
    Create internal/bundle/bundles.json with 2-space indent, lowercase keys:
    { "bundles": { "cloud": { "packages": ["aws", "gcp", "azure"] }, "dev": { "packages": ["playwright", "github", "cicd"] }, "context": { "packages": ["context-mode", "rtk", "serena"] } } }

    Create internal/bundle/bundles.go in package bundle. Import _ "embed" and "encoding/json" and "fmt". Embed bundlesData via //go:embed bundles.json directive on var bundlesData []byte. Define BundleDef struct with Packages []string json:"packages". Define BundleManifest struct with Bundles map[string]BundleDef json:"bundles". Implement LoadBundles() (*BundleManifest, error) that json.Unmarshal(bundlesData, &m) and returns error wrapped with "parsing bundles.json: %w". Implement (m *BundleManifest) Resolve(name string) ([]string, error) that looks up m.Bundles[name] and returns fmt.Errorf("bundle %q not found; available: cloud, dev, context", name) on miss.

    Create internal/bundle/bundles_test.go in package bundle_test. Tests: TestLoadBundles_OK verifies LoadBundles returns non-nil manifest with 3 bundles. TestResolveCloud verifies Resolve("cloud") returns ["aws","gcp","azure"]. TestResolveUnknown verifies Resolve("foo") returns non-nil error.
  </action>
  <verify>
    <automated>go test ./internal/bundle/... -v</automated>
  </verify>
  <acceptance_criteria>
    - internal/bundle/bundles.json exists and contains the literal string "context-mode"
    - internal/bundle/bundles.go contains "//go:embed bundles.json"
    - internal/bundle/bundles.go contains "func LoadBundles()"
    - internal/bundle/bundles.go contains "func (m *BundleManifest) Resolve"
    - go test ./internal/bundle/... exits 0 with all 3 tests passing
  </acceptance_criteria>
  <done>Bundle package created with embedded bundles.json; LoadBundles and Resolve work correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Add --bundle flag to installCmd and implement runBundleInstall</name>
  <files>cmd/install.go</files>
  <read_first>
    - cmd/install.go — read fully before modifying; understand current runInstall structure, goroutine+channel pattern, handleInstallError, svc wiring (lines 37-102)
    - .planning/phases/03-bundled-skills/03-PATTERNS.md §cmd/install.go — complete runBundleInstall implementation with sync.WaitGroup pattern
    - .planning/phases/03-bundled-skills/03-RESEARCH.md §2 Bundle Command §Parallelism Pattern — explains why errgroup is forbidden (D-04)
    - internal/registry/registry.go — check if gsd-core registry is already registered in NewRegistryManager()
  </read_first>
  <action>
    BLOCKING PRE-CONDITION (D-17): Before writing any code in this task, the executor MUST verify the actual install method used by the `gsd` package in the gsd-core registry. Fetch https://raw.githubusercontent.com/open-gsd/gsd-core/main/registry.json (using ctx_fetch_and_index or ctx_execute with an HTTP sandbox call — do NOT use curl/wget). Find the entry where "name" == "gsd". Read its "install.method" field. Document the finding as a comment at the top of cmd/install.go: "// CLI-03: gsd package uses method=<found-value> in gsd-core registry (verified <date>)". If the method is "github-release", note that the GitHubReleaseInstaller added in Plan 01 handles it. If the method is anything else, implement accordingly. Do NOT assume the method is "custom" — verify first. Only proceed with the implementation steps below after this verification is complete and documented.

    Modify cmd/install.go:

    1. Change installCmd Args from cobra.ExactArgs(1) to cobra.RangeArgs(0, 1). Keep all other cobra fields unchanged.

    2. In init(): add installCmd.Flags().StringP("bundle", "b", "", "Install a preset bundle (cloud, dev, context)").

    3. In runInstall(): move the name := args[0] line inside an else branch. At the top of runInstall, after reading the target flag, add:
       bundleName, _ := cmd.Flags().GetString("bundle")
       if bundleName != "" {
           // build svc (same wiring as below) then dispatch
           ...
           return runBundleInstall(cmd, bundleName, target, svc)
       }
       The svc wiring (store/reg/ad/svc construction) must be factored so it runs for both the bundle and single-package paths. Refactor: extract buildInstallService(target) (*service.InstallService, error) helper that returns the wired svc, or inline the wiring in both paths — inline is fine since it's 5 lines.

    4. Add bundleResult struct: name string, pkg *domain.Package, err error.

    5. Add function runBundleInstall(cmd *cobra.Command, bundleName, target string, svc *service.InstallService) error:
       - Call bundle.LoadBundles() — return error on failure
       - Call manifest.Resolve(bundleName) — return error on failure (already has descriptive message)
       - Allocate results := make([]bundleResult, len(pkgNames))
       - Declare var wg sync.WaitGroup
       - For i, name := range pkgNames: wg.Add(1), go func(idx int, n string) { defer wg.Done(); pkg, err := svc.Install(n, target); results[idx] = bundleResult{name: n, pkg: pkg, err: err} }(i, name)
       - wg.Wait()
       - Iterate results: for success print "  %s ✓\n" to stdout; for failure print "  %s ✗ %s\n" to stderr; count failed
       - Print summary: if failed == 0: fmt.Printf("%d/%d installed\n", len(pkgNames), len(pkgNames)); else: fmt.Fprintf(os.Stderr, "%d/%d installed — %d failed\n", len(pkgNames)-failed, len(pkgNames), failed); os.Exit(1) per D-05

    6. Add imports: "sync", "github.com/ejyle/agentkit/internal/bundle"
  </action>
  <verify>
    <automated>go build ./... && go test ./cmd/... 2>/dev/null; go vet ./cmd/...</automated>
  </verify>
  <acceptance_criteria>
    - cmd/install.go contains "cobra.RangeArgs(0, 1)"
    - cmd/install.go contains "func runBundleInstall"
    - cmd/install.go contains "sync.WaitGroup" (NOT "errgroup")
    - cmd/install.go imports "github.com/ejyle/agentkit/internal/bundle"
    - cmd/install.go does NOT import "golang.org/x/sync/errgroup"
    - cmd/install.go contains a comment documenting the verified gsd package install method (D-17 gate)
    - go build ./... exits 0
    - go vet ./cmd/... exits 0
    - Manual: agentkit install --help shows --bundle / -b flag in usage
  </acceptance_criteria>
  <done>--bundle flag wired; runBundleInstall uses sync.WaitGroup for best-effort parallel installs; exit code 1 on any failure per D-05; existing single-package install path unchanged; D-17 gsd registry method verified and documented before implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bundles.json → loader | Embedded at compile time; content is controlled by this repo — no runtime injection possible |
| cmd → goroutines | Goroutines invoke svc.Install() concurrently; each goroutine writes to its own index in the results slice |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Tampering | bundles.json at build time | accept | File is in this repo; build-time embedding means no runtime file substitution is possible |
| T-03-07 | Denial of Service | parallel Install goroutines | accept | Bundle size is bounded (3 packages per bundle); no unbounded fan-out |
| T-03-08 | Elevation of Privilege | os.Exit(1) in runBundleInstall | accept | Exit code 1 is the intended D-05 behavior; no privilege escalation |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new package manager installs in this plan |
</threat_model>

<verification>
go build ./... exits 0
go test ./internal/bundle/... exits 0
go vet ./cmd/... exits 0
agentkit install --help output includes --bundle flag
D-17 gsd registry method verified and documented as comment in cmd/install.go before any CLI-03 code was written
CLI-03 status documented in SUMMARY (gsd-core registry pre-registered; install gsd routes through existing path)
</verification>

<success_criteria>
agentkit install --bundle cloud resolves ["aws","gcp","azure"] from embedded bundles.json, launches 3 parallel svc.Install() goroutines, prints per-package result lines, and exits 0 (all success) or 1 (any failure). agentkit install gsd routes through the gsd-core registry without additional code changes. D-17 gsd install method verified before implementation.
</success_criteria>

<output>
Create .planning/phases/03-bundled-skills/03-02-SUMMARY.md when done.
</output>
