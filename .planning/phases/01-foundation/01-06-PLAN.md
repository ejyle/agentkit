---
phase: 01-foundation
plan: 06
type: execute
wave: 5
depends_on:
  - 01-03
  - 01-04
  - 01-05
files_modified: []
autonomous: false
requirements:
  - CLI-01
  - CLI-02
  - CLI-05
  - CLI-06
  - CLI-07
  - CLI-08
  - REG-01
  - REG-02
  - REG-05
  - REG-06
  - AST-01
  - MCP-01
  - MCP-03
  - MCP-05
  - MCP-06
  - MCP-07
  - SKL-01
  - SKL-02
  - SKL-03
must_haves:
  truths:
    - "agentkit install playwright --target claude resolves from agentkit-registry, runs npx adapter, writes ~/.claude.json, records to installed.json, prints success line"
    - "agentkit list shows playwright in the output after install"
    - "agentkit search playwright returns results from agentkit-registry"
    - "agentkit uninstall playwright removes the MCP config entry and records from installed.json"
    - "agentkit update playwright shows up-to-date or upgrade notice"
    - "~/.claude.json is valid JSON after every operation"
    - "No other keys in ~/.claude.json were modified by agentkit operations"
  artifacts:
    - path: "go.mod"
      provides: "All Phase 1 dependencies"
      contains: "github.com/ejyle/agentkit"
  key_links:
    - from: "agentkit binary"
      to: "~/.claude.json"
      via: "ClaudeCodeAdapter.WriteMCPConfig / RemoveMCPConfig"
      pattern: "mcpServers"
    - from: "agentkit binary"
      to: "~/.config/agentkit/claude/installed.json"
      via: "ConfigStore.RecordInstalled / RemoveRecord"
      pattern: "packages"
---

<objective>
Human verification checkpoint — confirm the complete Phase 1 walking skeleton and CLI surface
works end-to-end on real infrastructure. Automated tests already passed in 01-03 through 01-05.
This checkpoint verifies the full user experience against real registries and real config files.

Purpose: Automated unit tests use mocks. This checkpoint verifies behavior against the live
agentkit-registry and real Claude Code config paths.

Output: User confirms all 5 Phase 1 success criteria pass. Phase 1 is complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/SKELETON.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify Phase 1 success criteria end-to-end</name>
  <what-built>
    All Phase 1 CLI commands were implemented across plans 01-01 through 01-05:
    - agentkit install (with spinner, success line, conflict detection)
    - agentkit list (table with PACKAGE/VERSION/TYPE/TARGET/REGISTRY columns)
    - agentkit search (ranked results from agentkit-registry)
    - agentkit uninstall (non-destructive merge, D-09)
    - agentkit update (latest version comparison, D-08 auto-overwrite)

    The walking skeleton acceptance test in SKELETON.md specifies the exact expected output.
  </what-built>
  <how-to-verify>
    Run these steps in order. Before starting, note any existing content in ~/.claude.json
    so you can verify it is preserved.

    Step 1 — Build:
      go build -o agentkit ./...
      Expected: exits 0, no output

    Step 2 — Install:
      ./agentkit install playwright --target claude
      Expected: spinner phases visible, then:
      "✓ playwright@<version> installed → ~/.claude/skills/playwright/ (claude)" (or MCP path)

    Step 3 — Verify config written:
      cat ~/.claude.json | python3 -m json.tool
      Expected: valid JSON, mcpServers.playwright key present

    Step 4 — Verify installed.json:
      cat ~/.config/agentkit/claude/installed.json | python3 -m json.tool
      Expected: valid JSON, packages.playwright entry with name/version/type/install_path/installed_at/source_url/checksum

    Step 5 — List:
      ./agentkit list
      Expected: table with PACKAGE VERSION TYPE TARGET REGISTRY headers, playwright row visible

    Step 6 — Search:
      ./agentkit search playwright
      Expected: spinner, then ranked results from agentkit-registry, playwright in results

    Step 7 — Update:
      ./agentkit update playwright
      Expected: either "playwright is already up to date" or "⚠ playwright: upgrading X → Y"

    Step 8 — Uninstall:
      ./agentkit uninstall playwright --target claude
      Expected: "✓ playwright uninstalled (claude)"

    Step 9 — Verify clean uninstall:
      cat ~/.claude.json | python3 -m json.tool
      Expected: mcpServers.playwright key is GONE; all other keys in ~/.claude.json are unchanged

    Step 10 — Verify list is empty:
      ./agentkit list
      Expected: "No packages installed for target: claude" message

    Step 11 — Verify go test:
      go test ./... -count=1
      Expected: exits 0, all packages pass
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 11 steps pass. Otherwise describe which step failed and the exact output.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live registry fetch | Real network call to agentkit-registry GitHub repo during verify step |
| ~/.claude.json | Real user config file; verify step must not leave it in a corrupt state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | ~/.claude.json during verify | mitigate | User backs up file before running steps; uninstall step cleans up; post-verify JSON validity check |
| T-06-02 | Availability | live registry unavailable | accept | ETag cache fallback (REG-06) means search still works if registry is temporarily unavailable |
| T-06-SC | Tampering | npm/go package installs | accept | No new installs in this verification plan |
</threat_model>

<verification>
All 11 manual steps above pass. If any step fails, return to the relevant plan (01-03 for install
issues, 01-04 for list/search issues, 01-05 for uninstall/update issues) and fix before
re-running this checkpoint.
</verification>

<success_criteria>
User confirms all 5 Phase 1 success criteria from ROADMAP.md pass:
1. agentkit install playwright --target claude resolves, installs, writes ~/.claude.json, records installed.json, prints success line
2. agentkit list shows every installed package (none missing)
3. agentkit search playwright returns ranked results from all registries with source labels
4. agentkit uninstall playwright removes entry with no leftover artifacts
5. agentkit update shows up-to-date status or upgrades correctly
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-06-SUMMARY.md` when done.
</output>
