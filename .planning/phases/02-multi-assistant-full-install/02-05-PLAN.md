---
phase: 02-multi-assistant-full-install
plan: 05
type: execute
wave: 3
depends_on:
  - 02-02
  - 02-03
  - 02-04
files_modified: []
autonomous: false
requirements:
  - AST-02
  - AST-03
  - AST-04
  - AST-05
  - AST-06
  - MCP-02
  - MCP-04

must_haves:
  truths:
    - "agentkit install <name> --target copilot-cli completes without error and writes valid JSON to ~/.copilot/mcp-config.json"
    - "agentkit install <name> --target gemini completes without error and writes valid JSON to ~/.gemini/settings.json"
    - "agentkit install <name> --target codex completes without error and writes valid TOML to ~/.codex/config.toml"
    - "agentkit install <name> --target opencode completes without error and writes valid JSON to ~/.config/opencode/opencode.json"
    - "agentkit install <name> --method uvx fails with ErrUvxNotFound when uvx is absent; succeeds when uvx is present"
    - "agentkit install <name> --method docker fails with ErrDockerNotFound when docker is absent; runs docker pull on install"
    - "All Phase 1 commands (install, list, search, uninstall, update) still work after Phase 2 changes"
  artifacts:
    - path: ".planning/phases/02-multi-assistant-full-install/02-05-SUMMARY.md"
      provides: "Verification results for Phase 2"
  key_links:
    - from: "agentkit install --target copilot-cli"
      to: "~/.copilot/mcp-config.json"
      via: "NewAdapter('copilot-cli') → CopilotCLIAdapter.WriteMCPConfig"
      pattern: "copilot-cli"
    - from: "agentkit install --target opencode"
      to: "~/.config/opencode/opencode.json"
      via: "NewAdapter('opencode') → OpenCodeAdapter.WriteMCPConfig"
      pattern: "opencode"
---

<objective>
Verify Phase 2 end-to-end: all 5 new assistant adapters write correct config files, both new installers (uvx, docker) behave correctly, and Phase 1 functionality is unaffected. This is the human-in-the-loop checkpoint that confirms real-world behavior before Phase 2 is marked complete.

Purpose: Automated tests verify unit behavior; this plan verifies integration — that the adapter factory wiring, --target flag routing, and real config file writes all work together as expected.

Output: Phase 2 marked complete; 02-05-SUMMARY.md created.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-multi-assistant-full-install/02-CONTEXT.md
@.planning/phases/02-multi-assistant-full-install/02-01-SUMMARY.md
@.planning/phases/02-multi-assistant-full-install/02-02-SUMMARY.md
@.planning/phases/02-multi-assistant-full-install/02-03-SUMMARY.md
@.planning/phases/02-multi-assistant-full-install/02-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full test suite + build verification</name>
  <files></files>
  <read_first>
    - .planning/phases/02-multi-assistant-full-install/02-04-SUMMARY.md — confirm all adapter tests passed in Wave 2
  </read_first>
  <action>
    Run the full test suite and verify:
    1. go test ./... — all tests pass (zero failures)
    2. go build -o /tmp/agentkit-phase2-check ./cmd/agentkit — binary builds clean
    3. /tmp/agentkit-phase2-check install --help — displays --target flag with all 7 options (claude, copilot-cli, copilot-vscode, codex, gemini, opencode, pi)
    4. /tmp/agentkit-phase2-check install test-pkg --target invalid-target — exits non-zero with error message naming valid targets
    Capture all output. Report pass/fail for each check.
  </action>
  <verify>
    <automated>cd /Users/nithin/Ejyle/coding-agent-utils && go test ./... 2>&1 | tail -20</automated>
  </verify>
  <done>
    - go test ./... exits 0
    - Binary builds without error
    - --target flag help lists all 7 targets
    - Invalid --target exits non-zero
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All 5 new assistant adapters (Copilot CLI, Copilot VS Code, Gemini, Pi, Codex, OpenCode), 2 new installers (UvxInstaller, DockerInstaller), NewAdapter factory, and --target flag expansion. Full unit test coverage (8+ tests per adapter/installer).
  </what-built>
  <how-to-verify>
    Run these checks on your local machine. Each should produce the described result.

    **1. Full test suite (required)**
    ```
    cd <repo-root>
    go test ./... -v 2>&1 | grep -E "^(=== RUN|--- PASS|--- FAIL|FAIL|ok)"
    ```
    Expected: all lines show PASS or ok; zero FAIL lines.

    **2. Build + --target flag check (required)**
    ```
    go build -o /tmp/agentkit-check ./cmd/agentkit
    /tmp/agentkit-check install --help
    ```
    Expected: --target flag description includes copilot-cli, copilot-vscode, codex, gemini, opencode, pi.

    **3. Install routing to new adapters (spot check 2 of 5 targets)**
    Use a package that exists in the local registry (e.g., "playwright" from Phase 1 registry.json). Run:
    ```
    /tmp/agentkit-check install playwright --target gemini
    ```
    Expected: exit 0; ~/.gemini/settings.json exists and contains mcpServers.playwright entry with command/args.

    ```
    /tmp/agentkit-check install playwright --target codex
    ```
    Expected: exit 0; ~/.codex/config.toml exists and contains [mcp_servers.playwright] section.

    **4. uvx installer availability check**
    ```
    /tmp/agentkit-check install some-python-mcp --method uvx --target claude
    ```
    If uvx is NOT on your PATH: expected error contains "uvx not found on PATH" and install URL.
    If uvx IS on your PATH: expected install attempt (may fail on unknown package — that is fine).

    **5. Phase 1 regression check (required)**
    ```
    /tmp/agentkit-check install playwright --target claude
    /tmp/agentkit-check list
    /tmp/agentkit-check uninstall playwright --target claude
    ```
    Expected: all three commands succeed without errors. Phase 1 functionality is unaffected.

    **6. Pi skill path sanity check (LOW confidence path — per RESEARCH.md A4)**
    ```
    /tmp/agentkit-check install gsd --target pi
    ```
    If gsd is a skill-type package: check that skill files appear at ~/.agents/skills/gsd/ (not ~/.pi/skills/gsd/).
    If gsd is an MCP package: check ~/.pi/agent/mcp.json for the mcpServers entry.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all required checks pass.
    Or describe any failures (include the exact error output) so gaps can be planned.
    Note: Check 6 is informational — failure does not block approval if checks 1-5 pass.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| built binary → real filesystem | Verification writes to real ~/.gemini/settings.json and ~/.codex/config.toml on the developer machine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-13 | Tampering | Verification binary writes to real assistant config files | accept | Verification is intentional; files are user-owned; agentkit only modifies its own mcpServers keys; RemoveMCPConfig available for cleanup |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new installs in this plan |
</threat_model>

<verification>
Phase 2 complete when:
- [ ] go test ./... exits 0 — all unit tests pass (Task 1)
- [ ] Human approves all required checks in the checkpoint (Task 2 checks 1-5)
- [ ] No Phase 1 regression observed (list/install/uninstall --target claude still works)
- [ ] At least 2 new --target values produce valid config file writes in spot check
</verification>

<success_criteria>
Phase 2 is done when:
1. agentkit install --target copilot-cli/codex/gemini/opencode/pi each write the correct format config to the correct path (runtime-detected, not hardcoded) — per ROADMAP.md Phase 2 Success Criterion 1
2. agentkit install using uvx or docker method completes without error and produces a valid re-parseable config entry — per ROADMAP.md Phase 2 Success Criterion 2
3. All Phase 1 commands continue working (no regressions)
4. Full test suite green (go test ./... exits 0)
</success_criteria>

<output>
Create `.planning/phases/02-multi-assistant-full-install/02-05-SUMMARY.md` when done.
</output>
